# Dotfiles

Personal config files.

## Install

```sh
./install.sh
```

This links the tracked dotfiles into `$HOME`.
Existing files are backed up to `~/.dotfiles-backup/`.

Put machine-specific settings in `~/.zshrc.local` or `~/.vimrc.local`.

## Files

- `.vimrc`
- `.zshrc`
- `install.sh`
